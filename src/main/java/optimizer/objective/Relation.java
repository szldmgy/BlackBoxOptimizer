package optimizer.objective;

/**
 * Enum class describing the possible {@link Objective} types.
 */
public  enum Relation {
    LESS_THAN, GREATER_THAN, /*EQUALS,*/ MINIMIZE,MAXIMIZE,MINIMIZE_TO_CONVERGENCE,MAXIMIZE_TO_CONVERGENCE,;
}
